require("tiny-glimmer").setup({
  overwrite = {
    search = {
      enabled = true,
      default_animation = "pulse",
    },
    undo = {
      enabled = true,
    },
    redo = {
      enabled = true,
    }
  }
})
